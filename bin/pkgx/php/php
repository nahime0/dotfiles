#!/bin/sh

# Load directly the binary file, if it exists
if [ -f "$HOME/.pkgx/php.net/v*/bin/php" ]; then
  exec "$HOME/.pkgx/php.net/v*/bin/php" "$@"
else # Otherwise, load the pkgx script
  exec pkgx +php.net -- php "$@"
fi
